# Legacy: Gloomfall (Streamlit roguelike)

This folder holds the original single-file Streamlit prototype (`app.py`) that this
repository started from. It is kept for reference only and is **not** part of the
PWA build. Nothing in the root project imports it.

Run it the old way if you ever need to: `pip install -r requirements.txt && streamlit run app.py`.
