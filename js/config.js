/**
 * config.js — the one file to edit when you publish.
 *
 * APP_LINKS.web is the live address of this game (GitHub Pages). It is what
 * the QR code on the title screen encodes, so anyone who scans it lands on
 * the page and gets the right install option for their phone.
 *
 * When median.co gives you store listings, paste them below. The Install
 * dialog then sends Android users to Google Play and iPhone users to the
 * App Store instead of the browser install flow. Leave a field empty ('')
 * to keep the browser/PWA flow for that platform.
 */
export const APP_LINKS = Object.freeze({
  web: 'https://sultan-alkaabi-hub.github.io/Fantasy_Claude/',
  android: '',     // e.g. 'https://play.google.com/store/apps/details?id=co.median.android.xxxxx'
  ios: '',         // e.g. 'https://apps.apple.com/app/id0000000000'
  androidApk: '',  // optional direct .apk download (median "Download APK" link)
});

export const APP_NAME = 'Gloomfall';
